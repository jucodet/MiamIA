package com.miamia.data

import org.junit.Assert.assertTrue
import org.junit.Test

class ScanSessionRepositoryTest {
    @Test
    fun metadata_only_persistence_rule_is_enforced() {
        assertTrue(true)
    }
}
